# _legacy — Quarantine Log (W2-B)

**1 INVARIANT**: every path listed below still exists on disk, byte-for-byte, with only its location changed (`mv`, never copy or delete) — the live tree can be rebuilt without this directory, and this directory can restore any item to the live tree with one `mv` back.

**2 IMPOSSIBLE**: the live tree cannot compile or serve correctly while any of these paths sit where they used to — each one collides in name, role, or staleness with its replacement (a same-named target file, a same-role component, or a stale build cache the new toolchain would read instead of regenerating).

**3 CLASS**: this is a QUARANTINE, not a deletion and not a judgment. Nothing here is asserted to be wrong; each item is asserted only to be *superseded* by a specific named replacement, so a future reader can diff old vs. new rather than take this file's word for it.

**4 HONEST LIMIT**: Quarantine only. Nothing here is deleted and nothing here is referenced by the live tree. This file does not assert that any moved file is wrong — only that it is superseded. Restoring any item is one `mv` back.

## Moved items

| path in `_legacy/` | original path | reason | bytes | lines |
|---|---|---|---|---|
| `_legacy/components/TuvalMemorialLanding.tsx` | `components/TuvalMemorialLanding.tsx` | 594-line monolith, 8 `<section>` + footer + floating CTA in one file, 117 hardcoded Hebrew runs, no component boundaries — replaced by 13 props-driven sections in `components/sections/` | 32927 | 594 |
| `_legacy/components/Analytics.tsx` | `components/Analytics.tsx` | dead: 0 importers across 11 code files scanned, and self-disabling with both id constants empty — replaced by `components/Analytics.tsx` written fresh in W5 with its import asserted by a test | 1775 | 54 |
| `_legacy/app/page.tsx` | `app/page.tsx` | 7-line `'use client'` wrapper that made the entire page tree a client component — replaced by `app/[locale]/page.tsx` (server) plus a `/` → `/he` redirect | 160 | 7 |
| `_legacy/tailwind.config.js` | `tailwind.config.js` | JS config with a greyscale palette (no navy/indigo/gold) — replaced by `tailwind.config.ts` carrying the customer's HSL token set | 1593 | 56 |
| `_legacy/DEPLOYMENT_INSTRUCTIONS.txt` | `DEPLOYMENT_INSTRUCTIONS.txt` | 139 lines of deployment prose for the discarded structure — superseded by the W8 handoff set | 6318 | 139 |
| `_legacy/docs/ALL_PHOTOS_COMPLETE_SUMMARY.txt` | `docs/ALL_PHOTOS_COMPLETE_SUMMARY.txt` | describes the discarded structure; nothing imports it; duplicates facts (Instagram handle, life dates, unit) that now live in one typed constant each — kept for provenance, superseded by `REBUILD_STATUS_v1_00.md` | 11135 | 208 |
| `_legacy/docs/DEEP_ANALYSIS_REPORT.txt` | `docs/DEEP_ANALYSIS_REPORT.txt` | same as above | 2752 | 53 |
| `_legacy/docs/DIR_STRUCTURE.txt` | `docs/DIR_STRUCTURE.txt` | same as above | 3130 | 57 |
| `_legacy/docs/FILE_MANIFEST.txt` | `docs/FILE_MANIFEST.txt` | same as above | 9285 | 192 |
| `_legacy/docs/FIX_SUMMARY.txt` | `docs/FIX_SUMMARY.txt` | same as above | 4320 | 72 |
| `_legacy/docs/IMPLEMENTATION_COMPLETE.md` | `docs/IMPLEMENTATION_COMPLETE.md` | same as above | 9152 | 352 |
| `_legacy/docs/IMPLEMENTATION_SUMMARY.txt` | `docs/IMPLEMENTATION_SUMMARY.txt` | same as above | 5756 | 107 |
| `_legacy/docs/PHOTO_QUICK_GUIDE.txt` | `docs/PHOTO_QUICK_GUIDE.txt` | same as above | 5508 | 94 |
| `_legacy/docs/PROJECT_STRUCTURE.md` | `docs/PROJECT_STRUCTURE.md` | same as above | 5644 | 255 |
| `_legacy/docs/RAVID_PHOTOS_ANALYSIS.md` | `docs/RAVID_PHOTOS_ANALYSIS.md` | same as above | 6719 | 236 |
| `_legacy/docs/TUVAL_PHOTOS_ANALYSIS.md` | `docs/TUVAL_PHOTOS_ANALYSIS.md` | same as above | 9014 | 339 |
| `_legacy/docs/SESSION_PACKAGE_SUMMARY.txt` | `docs/SESSION_PACKAGE_SUMMARY.txt` | describes the discarded structure; nothing imports any of them; they duplicate facts (Instagram handle, life dates, unit) that now live in one typed constant each — kept for provenance, superseded by REBUILD_STATUS_v1_00.md. Carries a 7th copy of the Instagram handle `ravid._.t` — see ledger §OPEN item 1. | 7914 | 177 |
| `_legacy/.next-stale-next14/` | `.next/` | stale Next.js 14 build output (48 files, 22,708,148 B), gitignored and untracked — quarantined so the Next 15 install cannot read a stale manifest. Regenerated by the next build. | 22708148 (48 files) | n/a (binary/build dir) |
| `_legacy/app/page-w2-scaffold-placeholder.tsx` | `app/page.tsx` | W2 scaffold placeholder; superseded by `app/[locale]/page.tsx` once locale routing landed. The `/` → default-locale redirect is owned solely by `middleware.ts`. Renamed on the way in because `_legacy/app/page.tsx` is already taken by the ORIGINAL client-component page (W2-B, row above) — nothing was overwritten. | 2599 | 38 |

**Resolved discrepancy**: the original work order's list said `docs/` held 11 files; the directory actually held 12. This was a seat-side transcription error, not a decision to leave a file behind. It was closed by moving the 12th file, `docs/SESSION_PACKAGE_SUMMARY.txt`, into `_legacy/docs/` (row above). `docs/` is now empty (0 files); the empty directory itself was left in place, not removed.
